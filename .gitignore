# ============================================================================
#  Что НЕ попадает в публичный репозиторий GitHub Pages
# ============================================================================

# Админка. Это единственная защита доступа: нет файла — нет админки.
# НЕ УБИРАЙТЕ эту строку, иначе редактор базы окажется в открытом доступе.
admin.html

# Исходники подрядчиков: 219 МБ презентаций и KMZ. Публиковать незачем.
input/

# Отчёты сборки
logs/

# Скрипты сборки — работают только на вашей машине, на Pages бесполезны
build.ps1
fetch-geo.ps1
Портал.cmd
attach-cc-photos.ps1
retry-citix-photos.ps1
set-citix-mb-prices.ps1
fix-citix-black-photos.ps1
clear-citix-broken-photos.ps1

# Служебное
*.pptx
*.pptm
*.kmz
_*.html
Thumbs.db
desktop.ini
.DS_Store
